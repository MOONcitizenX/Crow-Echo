import { BaseElement } from '../../BaseElement';
export class Header extends BaseElement {
    constructor() {
        super({
            tag: 'header',
            className: 'header'
        });
    }
}
